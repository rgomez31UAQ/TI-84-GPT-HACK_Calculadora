0->N
0->S
" "->Str0

Lbl 1
ClrHome
Menu("ANDYPANDY","ANDYGPT",2,"SETTINGS",3,"EXIT",9)

Lbl 9
Stop

Lbl 3
Menu("SETTINGS","CONNECT",4,"DISCONNECT",5,"SETUP",6,"BACK",1)

Lbl 6
ClrHome
15->N
Send(N)
Disp "SETUP MODE"
Disp "CONNECT TO WIFI"
Disp "NAMED: CALC"
Pause
Goto 3

Lbl 4
ClrHome
0->N
Send(N)
Disp "CONNECTING..."
Pause
Goto 3

Lbl 5
ClrHome
1->N
Send(N)
Disp "DISCONNECTED"
Pause
Goto 3

Lbl 2
ClrHome
Disp "ANDYGPT"
Disp ">"
Input "",Str0
ClrHome
Disp "THINKING..."
2->N
Send(N)
Send(Str0)
0->S
Repeat S
Get(S)
End
Get(Str0)
ClrHome
Disp Str0
Pause
Goto 2
