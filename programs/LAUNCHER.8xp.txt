ClrHome
ClrDraw
" "->Str0
0->S
0->N

Lbl A
ClrHome
Menu("ANDYPANDY","ANDYGPT",G,"SETTINGS",K,"EXIT",X)

Lbl X
ClrHome
Stop

Lbl K
Menu("SETTINGS","CONNECT",C,"DISCONNECT",D,"SETUP",W,"BACK",A)

Lbl W
ClrHome
15->N
Send(N)
Disp "SETUP STARTED"
Disp " "
Disp "1.CONNECT TO WIFI"
Disp "  NAMED: CALC"
Disp " "
Disp "2.OPEN BROWSER"
Disp " "
Disp "3.ENTER WIFI INFO"
Pause
Goto K

Lbl C
ClrHome
0->N
Send(N)
Disp "CONNECTING..."
Disp " "
Disp "PLEASE WAIT"
Pause
Goto K

Lbl D
ClrHome
1->N
Send(N)
Disp "DISCONNECTED"
Pause
Goto K

Lbl G
ClrHome
Output(1,1,"ANDYGPT")
Output(3,1,">")
Input "",Str0
ClrHome
Disp "THINKING..."
2->N
Send(N)
Send(Str0)
0->S
Repeat S
Get(S)
End
Get(Str0)
ClrHome
Output(1,1,Str0)
Pause
Goto G
